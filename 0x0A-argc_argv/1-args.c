#include "main.h"

/**
 * main - starting point
 * @argc: argument counter
 * @argv: array pointer to the strings
 *
 * Return: 0
 */
int main(int argc, char *argv[] _attribute_((unused)))
{
	print("%d\n", argc - 1);

	return (0);
}
